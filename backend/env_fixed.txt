# Environment Configuration for XAMPP Local Development
# ===================================================
# Configuración optimizada para desarrollo local con XAMPP

# Environment
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=info

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=1

# Database - XAMPP MySQL (Configuración local)
# Variables individuales para la base de datos
DB_HOST=localhost
DB_PORT=3306
DB_NAME=trading_db
DB_USER=root
DB_PASSWORD=

# URL de conexión completa
DATABASE_URL=mysql://root@localhost:3306/trading_db
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# Redis (opcional para desarrollo local)
REDIS_URL=redis://localhost:6379/0
REDIS_CACHE_TTL=300

# External APIs (configuración para desarrollo)
ALPHA_VANTAGE_API_KEY=demo
FINNHUB_API_KEY=sandbox_key
YAHOO_FINANCE_ENABLED=true
NEWS_API_KEY=your_news_api_key

# AI/ML Configuration (optimizado para desarrollo)
MODEL_PATH=models/
AUTO_TRAINING_ENABLED=false
AUTO_TRAINING_INTERVAL_HOURS=24
DRIFT_DETECTION_THRESHOLD=0.1

# Reinforcement Learning (configuración básica para desarrollo)
RL_AGENT_TYPE=DQN
RL_TRAINING_EPISODES=100
RL_EVALUATION_EPISODES=5
RL_MODEL_SAVE_INTERVAL=50

# MetaTrader 4 (deshabilitado para desarrollo)
MT4_HOST=localhost
MT4_PORT=9090
MT4_ENABLED=false
MT4_MAGIC_NUMBER=12345

# Trading Configuration (configuración conservadora para desarrollo)
COMMISSION_RATE=0.001
MAX_POSITION_SIZE=0.05
RISK_FREE_RATE=0.02
MAX_DAILY_LOSS=0.05

# Security (configuración para desarrollo)
SECRET_KEY=dev-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60
ALGORITHM=HS256

# Monitoring (configuración básica para desarrollo)
PROMETHEUS_ENABLED=false
PROMETHEUS_PORT=9090
SENTRY_DSN=

# Email notifications (deshabilitado para desarrollo)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# Subscription System (configuración local)
SUBSCRIPTION_DATA_DIR=data/subscriptions
SUBSCRIPTION_LOGS_DIR=logs/subscriptions

# Default Admin User (creado por el script de base de datos)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_EMAIL=admin@aitraderx.com
DEFAULT_ADMIN_PASSWORD=admin123

# Development specific settings
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
LOG_FILE=logs/app.log
BACKUP_ENABLED=false 